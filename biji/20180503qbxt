#include c++
数据结构：
  数据结构本质是逻辑结构；
  单调栈：维护栈内元素单调性；
    例题：POJ2559
    首先对于每个下端点维护出可能成为答案的单调序列，其次每次读入之后把对应高度推进栈同时进行维护（可是为什么我一定要手写呢？？？），答案就是当前弹出高度和已有高度的最大值（幸好不用输出方案）；
    代码细节：最后添加一个0作为结束标志；优秀的复杂度O（N）；
